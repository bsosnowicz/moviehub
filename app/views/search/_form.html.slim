= form_with url:search_path, method: :get do |f|
    = f.search_field :q,value:params[:q], placeholder: "Search", class:"searchBar"
    = f.button "Search", class: 'searchButton'
    ul#suggestionsList class="suggestions-list"
